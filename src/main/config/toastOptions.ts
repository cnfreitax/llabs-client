import { ToastOptions } from 'react-toastify'

export const toastProps: ToastOptions = {
	position: 'top-right',
	autoClose: 5000,
	closeOnClick: true,
	pauseOnHover: true,
	draggable: true,
	progress: undefined
}
